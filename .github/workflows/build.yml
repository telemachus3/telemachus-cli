name: Build CLI

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Test CLI
        run: |
          python -m cli.main validate examples/geotab.jsonl || true